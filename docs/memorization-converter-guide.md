# Конвертер memorization.json ⇄ CSV

Этот документ описывает использование скрипта `tools/memorizationConverter.mjs`, который преобразует параметры растений из `src/data/json/memorization.json` в удобную для редактирования CSV-таблицу и обратно. Руководство подходит редакторам, которые предпочитают работать в Excel, LibreOffice Calc или Google Sheets.

## Возможности

- **`to-csv`** — считывает `memorization.json` и формирует таблицу с колонками параметров (`scientificName`, `family`, `lifeCycle`, `toxicity` и др.) и привязкой к изображению (`imageId`).
- **`to-json`** — принимает подготовленный CSV и обновляет `src/data/json/memorization.json`, сохраняя раздел `genus` и сортируя записи по идентификаторам.

> ℹ️ Скрипт изменяет только разделы `plantParameters` и `plants`. Массив `genus` копируется из существующего `memorization.json`, поэтому его можно поддерживать вручную независимо от таблицы.

## Требования

1. **Node.js 18 или новее.** Установите с <https://nodejs.org/>.
2. **Исходные данные.** Актуальный файл `src/data/json/memorization.json` либо подготовленный CSV.
3. **Файлы из репозитория.** Скрипт автоматически находит `src/data/json/memorization.json`, когда запускается из корня репозитория.

## Преобразование memorization.json → CSV

1. Выполните команду:

   ```bash
   node tools/memorizationConverter.mjs to-csv --output Memorization.csv
   ```

   При необходимости можно явно указать исходный JSON через `--input <путь>`.

2. Откройте `Memorization.csv` в редакторе таблиц. Разделитель — запятая.

## Преобразование CSV → memorization.json

1. Подготовьте CSV с теми же колонками, что генерирует команда `to-csv`.
2. Запустите:

   ```bash
   node tools/memorizationConverter.mjs to-json --input Memorization.csv
   ```

   При отсутствии параметра `--output` данные будут записаны в `src/data/json/memorization.json`. Скрипт сохранит текущее содержимое `genus`.

3. После успешного завершения появится сообщение вида `CSV успешно преобразован в JSON: src/data/json/memorization.json`.

> ⚠️ Скрипт перезаписывает `memorization.json`. Перед запуском сохраните резервную копию или убедитесь, что файл отслеживается Git.

## Формат колонок CSV

| Колонка | Содержимое |
| --- | --- |
| `id` | Идентификатор растения (`42`, `146_1` и т. п.). |
| `scientificName` | Научное название (обязательное поле). |
| `family` | Ботаническое семейство. Можно оставить пустым. |
| `lifeCycle` | Жизненный цикл (`annual`, `perennial`, `biennial`...). |
| `hardinessZone` | Зона морозостойкости (`H2`, `H5` и др.). |
| `light` | Режим освещения (`fullSun`, `partialShade`...). |
| `toxicity` | Набор тегов токсичности через `;`. Каждая запись указывается в формате `tag:level`, например `cat:3; skin:2`. Можно оставить пустым. |
| `additionalInfo` | Дополнительное описание. |
| `imageId` | Идентификатор изображения из каталога (`p14_0_1`). Пустая ячейка означает, что растение ещё не подключено к режиму заучивания. |

## Проверка результата

- Выполните `to-csv`, затем `to-json` на полученном CSV и сравните файлы `memorization.json`, чтобы убедиться в совместимости.
- Если команда сообщает об ошибке, сообщение содержит номер строки и причину (например, неверный формат токсичности или дублирующийся `imageId`).

Удачного редактирования данных! 🌿
