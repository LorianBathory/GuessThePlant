# Валидатор PlantData.json

Этот документ описывает использование скрипта `tools/plantDataValidator.mjs`, который проверяет и нормализует игровой каталог растений напрямую в формате JSON. Утилита подходит редакторам и разработчикам, которые поддерживают `src/data/json/plantData.json` вручную или выгружают его из собственной базы данных.

## Что делает скрипт

- **Проверяет структуру.** Скрипт убеждается, что каталог содержит корректные идентификаторы растений и изображений, отсутствуют дубликаты, а ссылки в `wrongAnswers` указывают на существующие записи.
- **Нормализует данные.** При необходимости генерирует стабильные `imageId` (формата `p<ID>_<вариант>_<номер>`), подставляет пути `images/...`, сортирует записи по идентификатору и очищает лишние кавычки или пробелы.
- **Преобразует табличные выгрузки.** Если на вход подать JSON с табличными строками (экспорт из SQLite/DBeaver, где каждая запись — бывшая строка `PlantData.csv`), скрипт сгруппирует изображения по растениям, назначит идентификаторы и сформирует каноничную структуру `plants`.
- **Синхронизирует метаданные изображений.** При обработке табличных JSON-выгрузок валидатор пересчитывает `number_of_images` по списку `images`, отбрасывает лишние значения из `image_id` и автоматически генерирует недостающие идентификаторы, а сводку поправок выводит в секции «Автокоррекции».
- **Валидация через загрузчик игры.** После нормализации утилита прогоняет данные через `deriveNormalizedPlantData()` и `buildGameDataForTesting()`, проверяя, что все изображения попадают в игровой бандл без потерь.

## Требования

1. **Node.js 18 или новее.** Скачайте дистрибутив с <https://nodejs.org/> и установите с настройками по умолчанию.
2. **Репозиторий GuessThePlant.** Достаточно скопировать директорию `tools/` и файлы из `src/data/json/`, если работаете в отдельной папке.
3. **Актуальный `plantData.json`.** Именно его (или его копию) будет проверять утилита.

## Быстрый старт

1. Откройте терминал и перейдите в корень репозитория (`GuessThePlant/`).
2. **Важно: запускайте скрипт через Node.js.** Команда `py -3 plantDataValidator.mjs` стартует Python и приведёт к ошибке `SyntaxError`. Используйте только Node, как показано ниже.
3. Запустите проверку без изменений файла:

   ```bash
   node tools/plantDataValidator.mjs
   ```

   В выводе появится сводка: количество растений, изображений и вопросов, а также распределение по уровням сложности.

4. Чтобы перезаписать `plantData.json` нормализованной версией, добавьте флаг `--write`:

   ```bash
   node tools/plantDataValidator.mjs --write
   ```

5. При необходимости можно указать входной или выходной файл:

   ```bash
   node tools/plantDataValidator.mjs --input MyPlantData.json --output normalized.json
   ```

   Команда прочитает `MyPlantData.json`, проверит его и сохранит нормализованный результат в `normalized.json`, не изменяя исходный файл.

6. Для экспорта из базы данных используйте параметр `--input`, указывая на файл, сформированный DBeaver/SQLite. Скрипт распознаёт табличный JSON (каждая строка включает колонки `id`, `(ru)`, `(en)`, `(nl)`, `(sci)`, `number_of_images`, `image_id`, `images`, `wrong_answers`, `difficulty`, `difficulty_modificator`, `extra` и т. п.) и преобразует его в финальный `plantData.json`:

   ```bash
   node tools/plantDataValidator.mjs --input PlantData-rows.json --output src/data/json/plantData.json
   ```

   В выводе появится количество обработанных растений. Если вместо сводки скрипт сообщает «строка N: не удалось определить названия колонок...», проверьте, что JSON содержит строку заголовка или секцию `columns` с именами всех столбцов `PlantData`.

## Параметры командной строки

| Параметр | Описание |
| --- | --- |
| `--input <путь>` | Использовать другой источник данных вместо `src/data/json/plantData.json`. |
| `--output <путь>` | Сохранить нормализованный JSON в отдельный файл. Имплицитно включает запись на диск. |
| `--write` | Перезаписать входной файл нормализованной версией. |
| `--help` | Показать краткую справку по скрипту. |

Параметры можно комбинировать. Если указать одновременно `--output` и `--write`, будет записан файл из `--output`, а исходный JSON останется без изменений.

## Типичный рабочий процесс

1. Обновите данные в `plantData.json` (например, экспортировав их из SQLite или отредактировав вручную).
2. Запустите `node tools/plantDataValidator.mjs --write`, чтобы автоматически пронормализовать структуру и сохранить изменения.
3. Выполните `npm run export:data` и `npm run validate:data`, чтобы пересобрать легаси-бандл и убедиться, что загрузчик успешно обрабатывает новые данные.
4. Просмотрите диффы и закоммитьте изменения в Git.

## Расшифровка типичных ошибок

| Сообщение | Что означает | Как исправить |
| --- | --- | --- |
| `Запись растения <ID> должна быть объектом.` | В разделе `plants` встретился не-объект. | Убедитесь, что каждая запись — JSON-объект с полями `id`, `names`, `images` и т.д. |
| `Растение <ID> содержит повторяющийся imageId ...` | Внутри одного растения дублируется идентификатор изображения. | Переименуйте один из дублей или удалите лишнюю запись. |
| `imageId ... уже используется другим растением` | Один и тот же `imageId` встречается у двух растений. | Обновите идентификаторы так, чтобы они оставались уникальными. |
| `В списке wrongAnswers ... указан неизвестный идентификатор ...` | `wrongAnswers` ссылается на несуществующее растение. | Добавьте пропущенную запись или исправьте идентификатор. |
| `Игровой бандл ссылается на imageId, которых нет в plantImages ...` | После нормализации какое-то изображение не попало в `plantImages`. | Проверьте массив `images` соответствующих растений и восстановите пропущенные элементы. |
| `строка N: не удалось определить названия колонок в табличном JSON...` | Валидатор не смог сопоставить значения с колонками (нет заголовков или метаданных `columns`). | Добавьте строку заголовка с названиями колонок либо экспортируйте JSON вместе с массивом `columns`. |

## Полезные советы

- Скрипт автоматически формирует пути `images/<имя>.JPG`, поэтому можно хранить в JSON только идентификаторы изображений.
- Если у изображения указана собственная сложность, она переопределит базовое значение растения.
- Дополнительные поля в объектах изображений (например, заметки) не обрабатываются: храните их отдельно, если нужно.
- Для быстрой проверки внешних данных (например, выгрузки из базы) используйте комбинацию `--input` + `--output`, чтобы не затронуть основное хранилище.

Удачной работы с каталогом! 🌱
