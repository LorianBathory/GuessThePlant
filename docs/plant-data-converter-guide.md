# Конвертер PlantData ⇄ CSV

Этот документ описывает использование скрипта `tools/plantDataConverter.mjs`, который преобразует игровой каталог растений между форматом `src/data/json/plantData.json` и удобным для редактирования CSV. Руководство подходит редакторам, которые не знакомы с Node.js и предпочитают пошаговые инструкции.

## Что делает скрипт

- **`to-csv`** — считывает `plantData.json` и выгружает данные о растениях в CSV по образцу `docs/sample.csv`.
- **`to-json`** — принимает такой CSV и сразу перезаписывает `src/data/json/plantData.json`, не создавая промежуточных бандлов или временных файлов. Скрипт автоматически нормализует уровни сложностей и идентификаторы изображений.

> ℹ️ Разделы с букетами (`bouquetQuestions.json`) и режимом заучивания (`memorization.json`) не затрагиваются. Скрипт работает только с каталогом обычных растений.

## Что понадобится

1. **Node.js 18 или новее.** Скачайте дистрибутив с <https://nodejs.org/> и установите с настройками по умолчанию.
2. **Сам скрипт и сопутствующие файлы.** Проще всего клонировать или скачать весь репозиторий (кнопка `Code → Download ZIP`) и распаковать его.
3. **Исходные данные.** Актуальный файл `src/data/json/plantData.json` либо подготовленный CSV в формате `docs/sample.csv`.

## Подготовка рабочего каталога

1. Создайте папку, например `GuessThePlant`.
2. Скопируйте внутрь:
   - директорию `tools/` целиком (в ней лежит конвертер);
   - папку `docs/` с примерами CSV;
   - подпапку `src/data/json/` или минимум файл `plantData.json`.

Пример структуры:

```
GuessThePlant/
├── tools/
│   └── plantDataConverter.mjs
├── docs/
│   └── sample.csv
└── src/
    └── data/
        └── json/
            └── plantData.json
```

## Как открыть терминал

- **Windows:** нажмите `Win`, введите «PowerShell» и откройте найденное приложение.
- **macOS:** вызовите Spotlight (`Cmd + Space`), напишите «Terminal» и запустите программу.
- **Linux:** откройте любимый терминал, например сочетанием `Ctrl + Alt + T`.

Перейдите в папку с конвертером. Примеры команд:

```powershell
cd C:\Users\Имя\Downloads\GuessThePlant
```

```bash
cd ~/Downloads/GuessThePlant
```

## Преобразование plantData.json → CSV

1. Убедитесь, что `src/data/json/plantData.json` доступен в рабочей папке.
2. Запустите команду:

   ```bash
   node tools/plantDataConverter.mjs to-csv --input src/data/json/plantData.json --output PlantData.csv
   ```

   - `--input` — путь к исходному JSON (можно указать абсолютный).
   - `--output` — имя CSV-файла, который будет создан.

3. По завершении увидите сообщение вида `Converted 200 plants to CSV: PlantData.csv`. Откройте файл в Excel, LibreOffice Calc или Google Sheets, выбирая разделитель «запятая» при необходимости.

## Преобразование CSV → plantData.json

1. Подготовьте CSV по образцу `docs/sample.csv`. Важно сохранить заголовок колонок и заключать значения с запятыми в кавычки.
2. Выполните команду:

   ```bash
   node tools/plantDataConverter.mjs to-json --input PlantData.csv
   ```

   Параметр `--output` больше не используется: данные всегда записываются в `src/data/json/plantData.json`.

3. После завершения скрипт сообщит `CSV успешно преобразован в JSON: src/data/json/plantData.json`. Файл уже отформатирован и готов к коммиту.

> ⚠️ Скрипт перезаписывает `plantData.json`. Перед запуском сохраните резервную копию или убедитесь, что изменения отслеживаются системой контроля версий.
> ℹ️ Записи в `plantData.json` сортируются по идентификаторам растений, поэтому порядок объектов может отличаться от исходного, даже если данные не менялись.

## Формат столбцов CSV

| Столбец | Что указать |
| --- | --- |
| `id` | Идентификатор растения (число или строка `100_1`). |
| `(ru)`, `(en)`, `(nl)` | Названия растения на русском, английском и нидерландском языках. Оставьте ячейку пустой, чтобы в `plantData.json` сохранилось пустое значение. |
| `(sci)` | Научное название. Заполняется вручную в JSON или CSV, скрипт сохраняет указанное значение без дополнительных источников. |
| `images` | Количество изображений (можно оставить пустым — скрипт посчитает сам). |
| `ID изображений` | Список идентификаторов изображений через запятую (`p5_0_1, p5_0_2`). |
| `Названия файлов` | Имена файлов изображений. Допускается как `Rose.JPG`, так и `images/Rose.JPG`. |
| `wrongAnswers` | Идентификаторы растений для неправильных ответов. |
| `Сложность` | Базовый уровень сложности (`Easy`, `Medium`, `Hard`). Если ячейка оставлена пустой, базовая сложность не назначается. |
| `Переопределения сложности` | Пары `imageId:Уровень` для изображений с отличной от базовой сложностью. |

> ℹ️ Пустые ячейки в таблице не приводят к ошибке: скрипт записывает пустую строку в JSON либо пропускает необязательное поле (например, базовую сложность).

## Проверка результатов

- Выполните `to-csv`, затем `to-json` на полученном CSV и сравните файлы: так вы убедитесь, что формат совместим.
- Если скрипт сообщает об ошибке, сообщение укажет строку и причину (например, несоответствие количества `imageId` и имён файлов).

## Частые вопросы

- **Нужно ли редактировать колонку `images` вручную?** Нет. Она нужна только для удобства проверки, а при импорте значение пересчитывается автоматически.
- **Нужно ли где-то хранить дополнительный файл для `(sci)`?** Нет. Конвертер читает и записывает значение из текущего JSON или CSV.
- **Что делать, если в CSV добавлены новые уровни сложности?** Добавьте их в `DIFFICULTY_LEVELS` в `tools/plantDataConverter.mjs`, затем повторите конвертацию.

Успешной работы с данными! 🌱
